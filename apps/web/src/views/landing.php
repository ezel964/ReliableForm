<?php

declare(strict_types=1);
?>
<section class="hero">
  <h1>Build forms people<br>actually finish.</h1>
  <p class="hero-sub">
    ReliableForm is a small, fast form builder: design a form in minutes, share one
    link, and get every response archived as a PDF with an email alert in your inbox.
  </p>
  <div class="hero-actions">
    <a class="btn btn-primary btn-large" href="/register">Get started — it's free</a>
    <a class="btn btn-ghost btn-large" href="/login">Log in</a>
  </div>
  <p class="demo-hint">
    Just exploring? Use the demo account:
    <code>demo@reliableform.dev</code> / <code>demo1234</code>
  </p>
</section>

<section class="feature-grid">
  <div class="card feature">
    <h2>9 field types</h2>
    <p>Text, paragraph, email, number, dropdown, multiple choice, checkboxes,
       date and star ratings — added with one click, no drag-and-drop gymnastics.</p>
  </div>
  <div class="card feature">
    <h2>One link to share</h2>
    <p>Every form gets a short public URL. No accounts needed for respondents,
       and submissions are validated server-side, every time.</p>
  </div>
  <div class="card feature">
    <h2>PDFs &amp; alerts, async</h2>
    <p>Each response is rendered to a downloadable PDF and emailed to you by
       background workers — your form stays fast even when we're busy.</p>
  </div>
</section>
