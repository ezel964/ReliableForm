<?php

declare(strict_types=1);
?>
<section class="hero">
  <div>
    <p class="hero-eyebrow">Every response archived, alerted &amp; delivered</p>
    <h1>Build forms people <em>actually finish</em>.</h1>
    <p class="hero-sub">
      Design a form in minutes, share one short link, and relax: every response
      is validated server-side, archived as a PDF, and lands in your inbox —
      processed by background workers so your form never slows down.
    </p>
    <div class="hero-actions">
      <a class="btn btn-primary btn-large" href="/register">Get started — it's free</a>
      <a class="btn btn-secondary btn-large" href="/login">Log in</a>
    </div>
    <p class="demo-hint">
      Just exploring? Use the demo account:
      <code>demo@reliableform.dev</code> / <code>demo1234</code>
    </p>
  </div>
  <div class="hero-visual" aria-hidden="true">
    <div class="mock-form">
      <span class="mock-badge">Published</span>
      <div class="mock-title">Customer feedback</div>
      <div class="mock-row">
        <div class="mock-label"></div>
        <div class="mock-input"></div>
      </div>
      <div class="mock-row">
        <div class="mock-label wide"></div>
        <div class="mock-input"></div>
      </div>
      <div class="mock-row">
        <div class="mock-label"></div>
        <div class="mock-stars">★★★★<span>★</span></div>
      </div>
      <div class="mock-btn">Submit</div>
    </div>
  </div>
</section>

<section class="feature-grid" aria-label="Features">
  <div class="card feature">
    <div class="feature-icon" aria-hidden="true">⊞</div>
    <h2>10 field types, zero fuss</h2>
    <p>Text, paragraph, email, number, dropdown, multiple choice, checkboxes,
       date, star ratings and file uploads — added with one click, reordered
       with two. Conditional logic shows the right question at the right time.</p>
  </div>
  <div class="card feature">
    <div class="feature-icon" aria-hidden="true">⎘</div>
    <h2>One link to share</h2>
    <p>Every form gets a short public URL and an embeddable iframe snippet.
       Respondents never need an account, and every answer is re-validated
       server-side — no malformed data, ever.</p>
  </div>
  <div class="card feature">
    <div class="feature-icon" aria-hidden="true">✓</div>
    <h2>PDFs, alerts &amp; webhooks</h2>
    <p>Each response is rendered to a downloadable PDF, emailed to you, and
       POSTed to your webhook — all by background workers, so submitting stays
       instant even when we're busy.</p>
  </div>
</section>
