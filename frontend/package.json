{
  "name": "reliableform-frontend",
  "private": true,
  "packageManager": "pnpm@11.7.0",
  "scripts": {
    "build": "pnpm -r --filter \"./packages/apps/*\" build && node scripts/write-manifest.mjs",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@rspack/cli": "^1.1.0",
    "@rspack/core": "^1.1.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0"
  }
}
